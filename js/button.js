messageSection.addEventListener('click', (event) => {
    //          if(event.target.tagName === 'BUTTON') {
    //           const button  = event.target;
    //           const li = button.parentNode;
    //           const ul = li.parentNode;
             
    //             if(button.innerText === 'remove') {
    //                 ul.removeChild(li);
    //         }   
    //             else if(button.innerText === 'edit') {
    //                 const entry = li.firstElementChild;
    //                 const input = document.createElement('input');
    //                 input.type = 'text';
    //                 input.value = entry.innerText;
    //                 li.insertBefore (input, entry);
    //                 li.removeChild(entry);
    //                 button.innerText = 'save';
    //         }
    //           else if(button.innerText === 'save') {
    //             const input = li.firstElementChild;
    //             const entry = document.createElement('entry');
    //             entry.innerText = input.value;
    //             li.insertBefore(entry, input);
    //             li.removeChild(input);
    //             button.innerText = 'edit';
    //         }
    //       }
    //     });